# 0.0.2 — OK 下方增加状态文字

## 范围

- 在居中大字 OK 下方增加一行小号灰色文字：「all systems operational」
- 纯静态 HTML + CSS 改动，无新增依赖
- 仍为单文件 `src/index.html`

## 关联输入

- `e000021` owner_input: "在居中大字 OK 下方增加一行小号灰色文字：all systems operational（仍为纯静态页面）。"
- `e000022` item_added (kind=iteration)

## 验收标准

1. 公网 URL 浏览器访问成功，HTTP 200
2. 大字 OK 仍居中，字体大小 72px
3. OK 下方显示「all systems operational」，小号灰色（font-size ≤ 18px，color 为灰色系）
4. CSS 样式正常加载，无 404
5. 浏览器控制台无错误
6. Kimi 视觉审查通过

## 技术方案概要

将 `<body>` 内单一 `<h1>` 改为 `<div>` 容器包裹 `<h1>OK</h1>` + `<p>all systems operational</p>`，p 标签设置较小字号和灰色。

## 实施任务拆解

### T1: 修改 `src/index.html`

- 在 body 内用 div 包裹现有 h1
- 添加 `<p>all systems operational</p>`
- p 标签 CSS：`font-size: 16px`，`color: #8b949e`（GitHub 灰色），`margin-top: 12px`
- div 内 flex 列布局居中

### T2: 自测验证

- Python http.server + curl 自测
- 确认 HTML 包含新文字，样式正确

### T3: 提交

- Git commit

## 风险

- 无

## 不做

- 后端服务、数据库、API
- JavaScript 框架
- 动态内容

## 待移交

- 无新增
